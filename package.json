{
  "name": "ziex_app",
  "type": "module",
  "scripts": {
    "build": "wrangler build",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "dependencies": {
    "ziex": "^0.1.0-dev.928"
  },
  "devDependencies": {
    "wrangler": "^4.78.0",
    "@zigc/cli": "0.15.20"
  }
}